{
  "name": "version-sensitive-app",
  "private": true,
  "description": "Sample app using JS APIs only available in newer Node — use with Runly to see old versions fail.",
  "type": "module",
  "scripts": {
    "test": "node --test test/",
    "runly": "node ../../dist/cli.js -c ./runly.config.mjs"
  }
}
